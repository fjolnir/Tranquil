#TQNumber {
    - to: b {
        list = #[]
        a = self
        until a >= b
            list push: a++
        ^list
    }
}

sum = (0 to: 1000000) reduce: `obj, accum=0 | obj+accum`

print("Sum: %@", sum)
